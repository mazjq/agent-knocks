# Rust build output (Cargo.lock IS committed - binary app)
/target/
rust/target/

# C# build artifacts / packages (legacy/csharp build.ps1 / package.ps1)
bin/
dist/

# Editor / OS junk
.vs/
*.user
Thumbs.db
desktop.ini
